@page "/"
@using RustRconServerManager.Frontend.Services
@inject NavigationHelper NavHelper

<div class="flex items-center justify-center min-h-screen bg-[rgb(var(--body-bg))]">
    <div class="tw-spinner" style="width: 40px; height: 40px;"></div>
</div>

@code {
    protected override void OnInitialized()
    {
        NavHelper.NavigateTo("dashboard", true);
    }
}
